inputs:
  - { name: age,        dtype: float64 }
  - { name: image_path, dtype: object }

target:
  name: target           # dx label: nv | mel | bcc | akiec | bkl | df | vasc
  dtype: object

categorical: []
drift_tolerance: 0.05
