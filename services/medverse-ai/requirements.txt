# MedVerse AI service — slim deps for the LangGraph + Groq stack.
# Deliberately omits sklearn/lightgbm/wfdb/pandas — those belong in the
# main backend's ML adapters, not in the agent loop.

fastapi
uvicorn[standard]

# LangGraph + LangChain core (the OOM-heavy stack we moved off Render)
langgraph
langchain
langchain-core
langchain-groq
langchain-huggingface
langchain-chroma

# Direct Groq client
groq

# Embeddings backend for Chroma RAG history (sentence-transformers
# downloads ~90 MB on first run; HF Spaces caches it under /root/.cache).
chromadb
sentence-transformers

pydantic
python-dotenv
tenacity
numpy

# Tabular ML adapters (fetal_health, parkinson_screener, etc.) — load
# pickled sklearn pipelines from models/. graph_factory's
# _augment_with_ml_models calls these per specialty; missing weights
# degrade silently (returns nothing). Pandas needed for the 1-row
# DataFrame the preprocessor expects.
scikit-learn
pandas
joblib
